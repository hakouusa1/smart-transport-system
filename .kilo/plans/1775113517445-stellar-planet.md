# Plan: Increase bus icon offset from 200px to 250px

## Problem
The bus icon in `driver_dashboard_screen.dart` is currently positioned 200px from the bottom. The user wants it to be 250px higher from the bottom.

## File to modify
`chauffeur_app/lib/screens/driver_dashboard_screen.dart`

## Changes

### 1. Update comment (line 303)
```
// 200px from the bottom (60px above the speed badge at bottom: 140).
```
→
```
// 250px from the bottom (110px above the speed badge at bottom: 140).
```

### 2. Update pixelShift value (line 306)
```
final pixelShift = screenH / 2 - 200.0;
```
→
```
final pixelShift = screenH / 2 - 250.0;
```

## Impact
- The `_adjustedCenter` method is called in 3 places (lines 224, 259, 693) — all will use the new offset automatically
- The bus marker will appear 250px from the bottom instead of 200px
